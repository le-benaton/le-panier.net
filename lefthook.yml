pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,mjs}"
      run: npx eslint {staged_files}
    format:
      glob: "*.{ts,tsx,mjs,css,json}"
      run: npx prettier --check {staged_files}
